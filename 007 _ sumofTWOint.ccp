#include <iostream>
using namespace std;

int main()
{
    int first_v,second_v,sum;
    
    cout << "enter two int :";
    cin >> first_v >> second_v;
    
    //sum action
    sum = first_v + second_v;
    
    //print sum
    cout << first_v << " + " << second_v << " = " << sum;
    
    return 0;
}
